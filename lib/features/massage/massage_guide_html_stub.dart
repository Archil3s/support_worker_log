import 'package:flutter/material.dart';

class MassageGuideHtml extends StatelessWidget {
  const MassageGuideHtml({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'The visual massage guide is available in the web app.',
      style: TextStyle(color: Color(0xFFCDD7F0)),
    );
  }
}
